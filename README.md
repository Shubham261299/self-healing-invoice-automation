# self-healing-invoice-automation
Browser automation with LLM-powered self-healing selectors. When portal UI changes break the bot, Claude analyzes the DOM and proposes replacement selectors at runtime — eliminating manual selector-mapping toil.
